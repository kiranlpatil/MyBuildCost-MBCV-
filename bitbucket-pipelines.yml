# This is a sample build configuration for JavaScript.
# Check our guides at https://confluence.atlassian.com/x/14UWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: node:8.8.1

pipelines:
  custom: # Pipelines that are triggered automatically
    master:
    - step:
        caches:
          - node
        script: # Modify the commands below to build your repository..
          - node -v
          - npm -v
          - npm install
          - npm rebuild
          - npm install gulp -g
          - npm install typings -g
          - typings install
          - gulp build.prod
        artifacts:
          - dist/**
    - step:
           script:
              - apt-get update # required to install zip
              - apt-get install -y zip # required for packaging up the application
#              - apt-get install python-pip -y
#              - pip install boto3==1.3.0 # required for codedeploy_deploy.py
              - zip -r dist_artifacts.zip dist/** # package up the application for deployment
              - echo "Please find below dist folder : "
              - ls
#              - curl -X POST "https://${BB_AUTH_STRING}@api.bitbucket.org/2.0/repositories/${BITBUCKET_REPO_OWNER}/${BITBUCKET_REPO_SLUG}/downloads" --form files=@"/opt/atlassian/pipelines/agent/build/dist_artifacts.zip" -u "AnilGadge:WYpAaN7ce8uwFzG3nzur"
#              - python codedeploy_deploy.py # run the deployment script
